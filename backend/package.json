{
  "name": "saas-backend",
  "version": "1.0.0",
  "description": "SaaS Platform Backend",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",

    "migrate": "psql $DATABASE_URL -f migrations/001_create_tenants.sql && psql $DATABASE_URL -f migrations/002_create_users.sql && psql $DATABASE_URL -f migrations/003_create_projects.sql && psql $DATABASE_URL -f migrations/004_create_tasks.sql && psql $DATABASE_URL -f migrations/005_create_audit_logs.sql",

    "seed": "psql $DATABASE_URL -f seeds/seed_data.sql",
    "seed:submission": "psql $DATABASE_URL -f seeds/seed_data.sql",

    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-validator": "^6.14.3",
    "helmet": "^6.0.1",
    "jsonwebtoken": "^9.0.0",
    "morgan": "^1.10.0",
    "pg": "^8.9.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
